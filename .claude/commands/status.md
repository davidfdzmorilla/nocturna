---
description: Estado del proyecto en 10 líneas: tarea en curso, siguiente, decisiones abiertas, git
---

Lee `docs/PLAN_TAREAS.md`, `docs/OPEN_DECISIONS.md` y ejecuta `git status --short` y `git log --oneline -5`. Responde con, como máximo:

- Tarea en curso (id, título, estado) o "ninguna".
- Siguiente tarea ejecutable (dependencias `done`).
- Número de decisiones abiertas sin resolver y las dos más antiguas.
- Cambios sin commit (número de ficheros).
- Últimos 3 commits.

Nada más. Sin recomendaciones no pedidas.
