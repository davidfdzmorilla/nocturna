---
description: Revisión bajo demanda del estado actual del árbol de trabajo (o de los ficheros indicados)
---

Delega en el subagente `reviewer` sobre `$ARGUMENTS` si se indican ficheros, o sobre `git diff` (staged + unstaged) si no. Muestra el informe completo. No modifiques nada.
