"""Utilidades de test compartidas que no son dobles de `LLMProvider`
(esas viven en `tests/fakes/`), sino ayudas para construir mensajes y
generadores falsos del propio `claude_agent_sdk` (ver `sdk_doubles.py`).
"""
