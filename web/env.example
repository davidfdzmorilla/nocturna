NOCTURNA_API_URL=http://localhost:8000

# Cero terceros, ni en build ni en runtime (ver CLAUDE.md). Los scripts de
# package.json ya la fijan; se documenta aquí por si se invoca `next`
# directamente (por ejemplo desde un IDE) sin pasar por pnpm run.
NEXT_TELEMETRY_DISABLED=1
