# Deuda técnica

Solo deuda real, detectada en código existente. Formato: `- <fichero> · <qué> · <por qué se aceptó> · <tarea en la que se detectó>`.

- `.claude/hooks/guard-bash.sh` y `.claude/hooks/stop-check.sh` · El marcador `.commit-approved` se borra al cierre del turno, impidiendo que `/commit-execute` lo use en modo EXECUTE. Además, `guard-bash.sh` inspecciona toda la cadena del comando e impide con falsos positivos: la ruta del marcador (`.claude/.commit-approved`) contiene "claude", y cualquier creación del marcador + commit a la vez se bloquea · El flujo de "preparar" vs "ejecutar" commit requería un mecanismo de persistencia entre turnos; descubierto solo en T00 · T00
- `backend/scripts/night_report.sql` · Duplica a mano `\set reserva 60000`, que debe seguir a `editor_reserve_tokens` en `pipeline.toml` · T60 reparametrizará `editor_reserve_tokens` por calibración; la desincronización no es hipotética. Solución: leer la constante desde `pipeline.toml` en el script o documentar explícitamente que ambos valores deben coincidir · T60
- `backend/alembic/versions/a318e7fd86a9_initial_schema.py` · Índice compuesto `agent_calls(run_id, agent)` sin el índice simple `agent_calls(run_id)` que `.claude/agents/database.md` pide para `tokens_used_for_run()` · Fase 1 ingesta ≤40 items/noche (≈15 000 al año); el índice compuesto cubre `run_id` por prefijo izquierdo. Revisar si se añade el simple cuando el volumen de `agent_calls` supere 100 000 filas o las queries `tokens_used_for_run` comiencen a ser lentas · T11
- `backend/alembic/versions/a318e7fd86a9_initial_schema.py` · Índices pospuestos: `items(status, fetched_at)` para `next_unread()` y `findings(run_id)` para `unpublished_for_run()` · Ambas son una consulta por noche sobre tablas que crecen ≤40 filas/noche. Revisar si se añaden cuando cualquiera de las tablas supere 50 000 filas (≈3 años de operación) o las queries comiencen a ser lentas · T11
- `backend/src/nocturna/infrastructure/arxiv/client.py` · Sin reintento ante 5xx de arXiv · Una noche con arXiv caído no ingesta nada y lo reporta. Deliberado en fase 1 para no dejar un bucle colgado de madrugada. · T20
- `backend/src/nocturna/infrastructure/arxiv/client.py` · **Los 4xx no se tratan ni se distinguen** · Método `_get` solo trata `>= 500` como `ArxivUnavailable`, cualquier `status_code >= 400` (406, 429, 400, etc.) pasa de largo con cuerpo vacío y revienta después en `parse_feed` como «la respuesta de arXiv no es XML válido». Observado: 2026-09-18 11:44 arXiv devolvió `406 Not Acceptable` (transitorio, reproducible después con la misma URL y User-Agent); el error reportado fue falso. Impacto: diagnóstico opaco, perder tiempo buscando un problema de parseo que no existe. Con un `429` sería peor: el mensaje ocultaría que estamos superando el límite de cortesía de arXiv. **SALDADA el 2026-09-18**, el mismo día: `_get` trata ahora cualquier `status_code >= 400` como `ArxivUnavailable` con el código en el mensaje, antes de parsear, distinguiendo en el texto el rechazo de la petición (4xx) del error de servidor (5xx). Sin reintentos nuevos. Congelado con tests de `406` y `429` que comprueban que la excepción es `ArxivUnavailable` y **no** `ArxivFeedError`, y que no hay reintento. Suite: 817 passed.
  Efecto colateral verificado y deseable: `ArxivClient.get_abstract`, que expone el servidor MCP a los agentes, capturaba `ArxivFeedError` para devolver `None` («id desconocido»), así que **un 4xx real le decía al agente `found: false`**, es decir, mentía. Ahora se propaga como fallo de herramienta, que es justo lo que el comentario de `arxiv_server.py` ya prescribía: «`is_error` se reserva para fallos de verdad (arXiv caído, id ilegible)». El comportamiento de la noche no cambia: `RunNight._run_ingest_phase` sigue capturando por `except Exception` y degradando a `PARTIAL` con motivo `ingest_error`, solo que ahora registra `ArxivUnavailable` con el código real en vez de un `ArxivFeedError` engañoso. · T20 · visto y saldado 2026-09-18
- `backend/src/nocturna/infrastructure/clock.py` · `zoneinfo` necesita la base de datos de zonas del sistema · En macOS y Linux con `tzdata` instalado está; una imagen Docker mínima la necesitaría (`apt-get install tzdata`). No anticipar despliegue ahora. · T30
- `backend/src/nocturna/infrastructure/llm/agent_sdk_provider.py` · `last_result` (último `ResultMessage` visto) **infracontaría** si algún día se pasan servidores MCP o hooks al proveedor · `query()` emite un solo `ResultMessage` por turno, pero `_internal/query.py` del SDK documenta que un frame cierra un turno, no necesariamente el run. Si se inyectaran herramientas (future API del SDK), habría múltiples frames y `last_result` sería incompleto. Revisar antes de dar herramientas a algún rol en T41. · T40
- `backend/src/nocturna/infrastructure/llm/agent_sdk_provider.py` · `aclose()` del `finally` está **fuera del `asyncio.timeout`** del llamador · si `aclose()` tarda 2+ segundos en matar el subproceso (normal), el reloj de pared de `item_timeout_s` o `hard_stop` se retrasa 2 segundos. T44 debe acotarlo con su propio timeout o aceptar el margen de ±2 s. · T40
- `backend/src/nocturna/infrastructure/llm/agent_sdk_provider.py` · Carrera conocida de `asyncio.timeout` · si una cancelación externa entra justo cuando expira el temporizador, CPython la convierte en `TimeoutError` y llama a `task.uncancel()`, así que el corte de `hard_stop` saldría como `LLMTimeout`. Estrecha (requiere sincronización de milisegundos), pero T44 no puede asumir lo contrario. · T40
- `backend/tests/` · `_request()` está duplicado en cuatro ficheros de test · `test_agent_sdk_env.py`, `test_agent_sdk_options.py`, etc. duplican la construcción de `AgentRequest` para tests. Candidato a `helpers/request_builder.py` cuando se reúnan los test suites de T41 · T40
- `backend/tests/pytest.ini` o equivalente · `pytest -m anyio` seleccionaría el test de humo por el mismo mecanismo que `-m db` · el riesgo es estrecho (solo si alguien lanza exactamente esa flag), congelado por `test_manual_markers_guard.py`. Riesgo residual si el SDK privado `_ALLOWED_MANUAL_MARKERS` cambia de símbolos. · T40
- `backend/src/nocturna/infrastructure/llm/agent_sdk_provider.py` · **Gasto de CLI invisible en `BudgetGuard`**: el cliente MCP interno (Haiku para control de sesión) gasta ~946 tokens/sesión en fase 1, contabilizados en `model_usage` pero no en `usage`. Mitigación: extracción con máximo entre `usage` y suma de `model_usage`, en todos los sitios que leen tokens (T40 implementó, T41–T44 heredan). Calibración de impacto real: T60 debe comparar `total_cost_usd` de Settings contra suma de `(costUSD por model_usage)` y ajustar `nightly_tokens` si hay déficit sistemático. · T40
- `backend/src/nocturna/application/budget.py` · **Llamadas que mueren antes de `ResultMessage`**: timeout de socket, cancelación externa previa a cualquier respuesta, kill del proceso — ninguno trae `usage` ni `model_usage`. Contabilización actual: 0 tokens. Cifra de fuga posible: ~600.000–800.000 tokens/noche si arXiv muere o la red falla, acotada por `CALL_LIMIT_REACHED` (80 llamadas máximo). Mitigación (a) implementada en T44: contador de fallos consecutivos (`max_consecutive_failures = 5`). **Alcance real más estrecho de lo que el comentario dice**: acota fallos **monótonos** (~5 por fase, ~10 por noche máximo, porque el contador se reinicia entre fases A y B), pero **no** fallos intermitentes (red inestable, 50% de llamadas muertas): con fallos alternos el contador se reinicia y la fuga sigue acotada solo por `CALL_LIMIT_REACHED` (80 llamadas/rol). Sin datos reales de T60 no se refina más. Hoy: fuga mapeada, mitigada, vigilada en T60. · T40/T44
- `backend/src/nocturna/application/budget.py` · **Gasto de sesión MCP no predecible desde `pipeline.toml`**: ~946 tokens/sesión es coste del CLI, no del usuario. En fase 1 con hasta 82 sesiones/noche (40 ítems × 2 turnos + Editor), suma ~77.572 tokens invisibles (20,5% de déficit). **No configurable**. Calibración: T60 vs Settings > Usage. Identidad de validación: `total_cost_usd == sum(costUSD en model_usage)` (si diverge, hay tercer sumidero). · T40
- `backend/src/nocturna/infrastructure/llm/agent_sdk_provider.py` · **`last_result` sobrescribe frames previos: se pierde el gasto de todos menos el último**. Contexto: `query()` emite múltiples `ResultMessage` (uno por turno/frame). Hoy se guarda solo el último en `last_result`, desechando el gasto de los anteriores. Fase 1 usa `tools=[]` (sin herramientas) → un solo frame/sesión → inerte. Riesgo activado: cuando T41 inyecte `mcp_servers` o `allowed_tools` no vacíos, el modelo llamará herramientas, generará múltiples frames, y `last_result` contendrá solo el gasto del frame final. Todos los frames anteriores (y su gasto) se pierden de forma irrecuperable. El revisor lo califica como «residual con más riesgo futuro de los tres». Solución: acumular en lugar de sobrescribir; o lanzar excepción si hay múltiples frames. **Obligatorio revisar antes de que T41 pase `mcp_servers` a ningún rol.** · T40
- `backend/src/nocturna/infrastructure/llm/agent_sdk_provider.py` · **Fallos previos a `ResultMessage`: pérdida de gasto irrecuperable**. Si `query()` lanza excepción antes de recibir el primer `ResultMessage` (timeout de red, cancelación durante `await`), no hay `usage` ni `model_usage`. El gasto de ese intento puede haber sido substantial (carga de caché, inicio de herramienta) pero no hay fuente de verdad para leerlo. Contabilización actual: 0 tokens. No hay solución en esta capa (no hay datos). Mitigación: que T44 implemente contador de fallos consecutivos (si fallan 5 seguidos, cierra como `partial`) o contabilidad pesimista. · T40
- `backend/src/nocturna/infrastructure/llm/agent_sdk_provider.py` · **Contabilización de `webSearchRequests`**: el SDK puede emitir búsquedas que se facturan por petición, no en tokens. Cero en fase 1. Si T41+ activa búsqueda, este contador no sería capturado en `BudgetGuard` (que cuenta solo tokens). Revisión necesaria. · T40
- `backend/src/nocturna/application/` · **Regla AST `run_agent` exigida**: `test_llm_call_sites.py` mapea todas las llamadas a `run_agent` en la codebase y verifica que viajen por `BudgetGuard.authorize` antes. La regla es **evasible con alias** (`call = self._provider.run_agent`); el revisor lo demostró. No es una garantía, es un detector de descuidos documentado en su docstring. · T41
- `backend/alembic/env.py` · `fileConfig(config.config_file_name)` con `disable_existing_loggers=True` (default) desactiva todos los loggers `nocturna.*` no declarados en `alembic.ini` cuando se importa `env.py` desde tests. T41 parchea con `monkeypatch`; T42 suma más loggers y el coste crece a tres líneas · Corrección real: `fileConfig(..., disable_existing_loggers=False)` o evitar `fileConfig` cuando se importa fuera de `alembic upgrade` · T42
- `backend/src/nocturna/application/` · **Contador de unidades de trabajo**: verifica que la cantidad de transacciones **no se reduzca** respecto a la baseline. Mide `query()` en `test_engine.pool` y comprueba `checkedout() == 0` durante `run_agent`. No detecta si una transacción se **expande** (retiene conexión más segundos). Solo la sonda de pool recién añadida en T41 cubre expansiones. · T41
- `backend/src/nocturna/application/budget.py`, `application/use_cases/read_item.py` · **Tres huecos de contabilidad de una sola llamada, conocidos y acotados**: (a) base de datos caída justo durante `record_call` dentro del timeout de `hard_stop` — `AgentCall` no se persiste, tokens se pierden; (b) `BaseException` que no sea `CancelledError` (ej. `KeyboardInterrupt`) durante la contabilización — fuga de esa llamada; (c) Ctrl-C durante `run-item` que deja `Run.status = RUNNING` sin `finished_at` — bloquea la noche siguiente por `uq_runs_status_running`. Este último es anterior a T41 y solo afecta al camino interactivo. T44 debe retomar huérfanos o fallar ruidosamente. · T41
- `backend/src/nocturna/application/use_cases/` · **Deuda de T42 saldada en T43**: T42 detectó que `_popularize_one_reading` devuelve `COMPLETED`, correcto solo porque era la última etapa. T43 lo arregla: `_edit_one_night` es ahora la única función que devuelve `COMPLETED`, las demás devuelven su desenlace parcial. T44 orquesta llamándolas en orden. Arreglado: 2026-09-17 · T42/T43
- `backend/src/nocturna/application/use_cases/` · **Deuda de T41 saldada por T44**: un `KeyboardInterrupt` deja el `Run` en `RUNNING`. La invocación siguiente de `run-night` adopta ese `Run` huérfano y lo cierra como `KILLED` antes de abrir uno nuevo. El camino de `run-item` no cambia (sigue adoptando el vivo). Arreglado: 2026-09-17 · T41/T44
- `backend/src/nocturna/application/use_cases/` · `item_timeout_s = 180` se comparte entre Reader y Popularizer, pero la salida del Popularizer es ~3× más larga (hasta 660 palabras). **Parcialmente saldada por T43**: el Editor ahora tiene `editor_timeout_s = 300` (5 min), diferenciado. Reader y Popularizer siguen compartiendo 180 s. Candidato a separarse aún más en `pipeline.toml` si T60 mide asimetría de timeouts entre agentes. · T42/T43
- `backend/tests/manual/` · **Gasto lateral del CLI no está atado a versión**: los humos `test_read_item_smoke.py`, `test_sdk_smoke.py`, `test_popularize_smoke.py` registran versión de SDK y CLI (2026-09-17: SDK 0.2.153, CLI 2.1.274), pero ningún dato de calibración (ej. `tokens_in`, `tokens_out` en `AgentCall`) está indexado a esas versiones. El CLI gastó 929 Haiku en T40, 1.163 en T41 (+234 tokens), cambio invisible en `BudgetGuard` que causó fallo del humo de T40. **Cada línea de calibración de T60 debe anotar la versión del CLI contra la que se midió**, sin eso datos históricos son inútiles si el CLI cambia. La cifra: ~82 sesiones/noche × ~1.163 Haiku = ~95.366 tokens/noche invisibles (31,8% del presupuesto). · T42
- `backend/src/nocturna/infrastructure/llm/agent_sdk_provider.py` · **Gasto lateral del CLI (Haiku) no configurable y variable**: ~1.163 tokens/sesión es un coste que el CLI de Claude Code incurre por su cuenta en cada sesión, no del prompt ni del modelo que pedimos. **Para qué lo usa, no lo sabemos**: el pipeline corre con `tools=[]` y `mcp_servers={}`, así que no es descubrimiento de herramientas ni MCP. En fase 1 con hasta 82 sesiones/noche, suma ~95.366 tokens invisibles en `budget.py` (no aparecen en `usage`, solo en `model_usage`). Contabilizado con máximo entre `usage` y suma de `model_usage`, así que no hay pérdida de dinero, pero: (a) son el **31,8%** de `nightly_tokens` (95.366 de 300.000), o el **39,7%** del pool de Reader y Popularizer, y no se controlan desde `pipeline.toml`; (b) **varía entre versiones del CLI sin que cambie nada nuestro**: de 929 a 1.163 tokens (+25,2%) entre humo de T40 (2026-09-16) y humo de T41 (2026-09-17), SDK fijo en 0.2.153. **Además, varía entre llamadas dentro de la misma versión**: T43 observó CLI 2.1.274 gastando 929, 1.163, 1.240 Haiku en tres llamadas consecutivas, **sin correlación clara con tamaño del prompt** (Popularizer 2.130 tokens cache_creation gastó menos Haiku que Editor 1.889). Variabilidad por causa desconocida, no predecible; (c) se debe validar en T60 contra `total_cost_usd` de Settings > Usage, usando la identidad `total_cost_usd == sum(costUSD en model_usage)` como detector de tercer sumidero si existe. **Cada calibración de T60 debe registrar versión de CLI contra la que se midió.** · T42/T43
- `backend/src/nocturna/application/use_cases/edit_night.py` · **Sin regresión que congele sobrevivencia de gasto ante `IntegrityError`**: `EditNight` persiste `Finding` tras `publish()`. Si `save(finding)` levanta `IntegrityError` (ejemplo: violación de constraint único que no predijimos), la transacción de persistencia hace rollback, pero `AgentCall` ya está grabada en su propia transacción (T30, ADR 0005 § 8). Gasto se cuenta, `Finding` no se crea, `Item` queda en `READ`. **Garantizado por `AgentRunner` + `record_call` en transacción propia**, pero ningún test de `EditNight` congela explícitamente esa propiedad. Candidato: test defensivo `test_edit_night_integrityerror_persists_call.py` que simule fallo en persist y verifica `AgentCall.status == ok` y tokens contabilizados. · T43
- `backend/src/nocturna/application/use_cases/edit_night.py` · **`EditNight(run_id)` desacoplada del `BudgetGuard`**: el constructor acepta `run_id` independientemente del `run_id` que vigila `BudgetGuard` (que viene de `w.runs.current()`). Hoy `cli.py` pasa siempre el mismo `run_id`, así que es inerte. Riesgo futuro: si T44 llama `EditNight` con otro `run_id`, el gasto se contabiliza en un Run mientras los candidatos se publican en otro. Prevención: `EditNight.run_id` debe coincidir con `guard.current_run_id` al inicializar, aserto explícito. · T43
- `backend/src/nocturna/application/budget.py` · **`timeout_for_call(role=None)` parámetro opcional no por diseño**: se mantiene la firma con `role` opcional solo para no tener que reescribir la suite de T30, que tiene tests de presupuesto que no pasan rol. En producción hay un único llamante (`runner.py`) que siempre pasa el rol. Refactor futuro: `runner.py` puede exigir `role` obligatorio, y eliminar la rama `None` de `timeout_for_call`. · T43
- `backend/tests/manual/test_edit_night_smoke.py` · **Cruzar entidades de dominio por identidad, no por igualdad**: el test comparaba `Finding` publicados mediante `in result.published` sobre instancias `@dataclass(slots=True)`, que por defecto usan igualdad de dataclass. Los `Finding` del repositorio son rehidratados y mutados (`confidence`, `published_at`); los construidos en test siguen sin mutar. Comparación fallaba siempre que se publicaba (pertenencia falsa, mandaba cada hallazgo a rama equivocada). **Lección general**: cuando un lado de la comparación ha pasado por repositorio o ha sido mutado, cruzar por `id`, nunca por igualdad de dataclass. Tests de T43 ya lo hacían correctamente (por `item_id`); solo el humo no. Descubierto y arreglado en rama `fix/t43-smoke-assert`. · T43
- `backend/src/nocturna/domain/entities.py` · **Docstring de `Run` promete lo que T44 no hace**: el docstring afirma «va incrementando los contadores a medida que avanza el pipeline», cuando en realidad se escriben una sola vez al cierre. Si el proceso muere entre fases (Reader completado, Popularizer en vuelo), el `Run` huérfano queda con `items_read > 0` pero contadores de lectura en 0, aunque haya `Reading` y `Finding` persistidos. El revisor recomienda **corregir el docstring del dominio**, no persistir por fase (añadiría transacciones sin ganancia). Registrado para futura lectura; no toques el código. · T44
- `backend/src/nocturna/application/use_cases/` · **El camino `FAILED` de `_finish_run` no pasa `notes` ni contadores**: una noche fallida cierra sin métricas ni rastro del tipo de excepción en base de datos. Solo se graba `status = failed` y `finished_at`. Sin datos real de T60, no se refina. · T44
- `backend/tests/db/` · **Cobertura de CLI de `run-night` vive bajo `-m db`**: si la suite se corre sin compose (`-m 'not manual'`), `_run_night_for_real` queda sin ningún test. Candidato: mover las pruebas que no toquen persistencia fuera de `-m db`. **Refactor de tests, no calibración: va a T61.** · T44
- `backend/tests/test_api_error_handling.py` · **Mitad del manejador de excepción no comprobada**: verifica que la respuesta sea `{"detail": "internal error"}` sin debug, pero no que la traza completa llegue al log (`logger.error(..., exc_info=exc)`). Con `caplog` es una línea. Deuda menor, pero segundo pasada de revisión lo señaló. · T50
- `backend/tests/test_api_error_handling.py` · **Tests resuelven `Settings` contra entorno real**: al llamar `create_app()` sin argumento, resuelven `get_settings()` desde variables de entorno. Si `NOCTURNA_CORS_ORIGINS` está mal formada en el `.env`, fallan con `SettingsError` antes de que corran, enmascarando lo que prueban. Falla ruidoso, no silencioso, pero candidato a `create_app(settings=Settings(...))` con valores fijos. · T50
- `backend/tests/test_api_error_handling.py` · **Docstring mezcla dos mutaciones distintas**: con `debug=False` (default), el handler devuelve texto plano `Internal Server Error` sin traza; con `debug=True`, traza completa (6.037 bytes, con `RuntimeError` y DSN). Son dos garantías distintas y conviene separarlas en dos tests o precisar el docstring. · T50
- `backend/scripts/seed_demo.py` · **No es idempotente**: segundo lanzamiento falla con `IntegrityError` sin corromper nada (todo dentro de `unit_of_work`). Aceptado para una herramienta de mano con guarda explícita `NOCTURNA_ALLOW_SEED=1`. Si se ejecuta varias veces, usar `--reset` o borrar las filas manualmente. · T50
- `backend/src/nocturna/api/routes/findings.py` · **`page` sin tope es un BUG, no una calibración** (etiquetado T50/T60 por error). La revisión encontró que `?page=100000000000000000000` tumbaba la API con `NumericValueOutOfRange` (`bigint out of range`): cualquiera con una URL forzaba un 500 y una traza en el log del backend. Se arregló en la web (`parsePageParam` exige `/^\d{1,6}$/` y acota a `MAX_PAGE = 999_999`, congelado con tests), pero **la validación del backend sigue sin tope**: un cliente que no sea esta web reproduce el fallo. Mensaje literal observado: `sqlalchemy.exc.DataError: (psycopg.errors.NumericValueOutOfRange) bigint out of range` con `[parameters: {'param_1': 20, 'param_2': 1999999999999999999980}]`. **Arreglar como tarea aparte inmediata** (una línea: `Field(ge=1, le=999_999)` en el schema de entrada). No pertenece a T60. · T50 · recomendado como tarea aparte inmediata 2026-09-18
- `web/tests/guards/no-claude-in-web.test.ts` · **Dos huecos conocidos**: (a) la allowlist concede el término `claude` a fichero completo justo en `lib/api/client.ts`, que es el único módulo que hace `fetch` —un `fetch("https://claude.ai/…")` ahí pasaría, aunque `api.anthropic.com` seguiría cazándose por "anthropic"—. Acotar por fragmento exacto o por número máximo de ocurrencias lo cerraría; (b) el escaneo cubre `package.json` y `src/**`, pero **no** cubre `next.config.ts` ni ficheros de configuración en la raíz (`.eslintrc`, etc.), donde cabría un `rewrites` hacia un host externo. Y ya que el fichero existe, ahí encajarían dos invariantes que hoy no congela nada: que no exista `src/app/api/**` (rutas de API en cliente) y que no aparezca `"use server"` ni `NEXT_PUBLIC_` en ficheros de librería (son telltales de incrustación de gasto en bundle). · T51
- `web/app/components/pagination.tsx` · **`aria-current` en paginador**: se quitó del `<p>` del contador porque el contador no es un enlace. Si algún día hay enlaces numerados por página (nav horizontal), ahí sí corresponde `aria-current="page"` en el enlace activo. Nota: `pnpm run lint` no lo detecta; Lighthouse accesibilidad pasó 100/100 porque el uso erróneo no aparecía en el HTML final. · T51
- `docs/adr/0005-control-de-gasto.md` · **Enmienda huérfana en § 10 (timeout rol-dependiente)**: ADR 0005 predijo timeout del Editor de 300 s; la implementación T43 coincidió. Pero T60 calibró posteriores cambios (cfg-2026-09-18) que requerirían revisión del § 10 si hubiera divergencia; enmienda documentada solo en ARCHITECTURE.md, sin ADR que la supersede. Regla de proceso: **si un ADR queda desactualizado, se escribe un ADR nuevo que lo supersede; documentar solo en texto no es opción.** · T60
- `docs/adr/0008-el-editor-llamada-n1-y-estimacion-de-coste.md` · **Enmienda de § 2 (estimación lineal)**: ADR 0008 propuso `4000 + 700×N` tras dos observaciones manuales (T43, 3 candidatos). T60 observó ejecución real (14 candidatos) y recalibró a `2500 + 850×N` (cfg-2026-09-18), validado sobre rango 1–40 candidatos. Enmienda documentada solo en CALIBRACION.md y ARCHITECTURE.md, sin ADR que supersede. Regla de proceso: **el ADR del cierre de T60 debe superseder formalmente ambos apartados (0005 § 10 y 0008 § 2) con sus valores finales y justificación de regresión.** · T60

---

## Deuda técnica cerrada

- `backend/src/nocturna/infrastructure/db/repositories.py` · **`unpublished_for_run` ordenado por `id`**: `SqlAlchemyFindingRepository.unpublished_for_run(run_id)` devuelve candidatos del `Run` ordenados por `id` (determinista). En T43, esos candidatos viajan al Editor, que es la llamada más cara de la noche. El orden `id` (cronológico de creación, no de interés) afecta al prompt del Editor: entrada distinta → gasto distinto. Se eligió `id` en lugar de `interest_score DESC` para no precodificar una política de priorización. **Arreglado en T43**: 2026-09-18 se añadió `ORDER BY id` explícito en la consulta para garantizar determinismo y documentación.
