# Decisiones abiertas

Formato: `- [ ] <tarea> · <pregunta> · opciones vistas: … ` → al resolver: `- [x] … · resuelto <fecha>: <respuesta>`.

- [x] T00 · Nombre del proyecto y del paquete Python · resuelto 2026-09-15: **Nocturna**, paquete `nocturna`
- [ ] T00 · ¿Qué estrategia de ramas sigue el proyecto? · opciones vistas: rama por tarea + PR a `main` (practicado en T00); commits directos en `main`
- [ ] T00 · ¿Qué licencia lleva el repositorio? · opciones vistas: sin licencia (privado), MIT, AGPL
- [ ] T00 · ¿El repositorio es público en GitHub o privado? · opciones vistas: público, privado
- [x] T01 · `.claude/agents/` ya está completo en el commit inicial: ¿se cierra T01 como verificación de los ocho ficheros contra `AGENTS.md`, o se reescriben? · resuelto 2026-09-16: cierre como verificación documentada más un delta mínimo de seis ediciones; los ocho ficheros ya satisfacían rol, herramientas y reglas, pero la definición de `backend` no alcanzaba para delegar T02
- [ ] T01 · ¿Qué subagente tiene la propiedad de `.claude/agents/`, `.claude/commands/`, `.claude/skills/` y `.claude/hooks/`? · opciones vistas: `docs-keeper` (practicado en T01, por ser markdown de proceso), `backend`, subagente nuevo
- [ ] T01 · `Bash(git commit*)` no está en `allow` ni en `deny` de `.claude/settings.json`, así que el commit dispara prompt al autor: ¿es deliberado como refuerzo de "preparar ≠ ejecutar"? · opciones vistas: dejarlo así, añadirlo a `allow`
- [ ] T01 · `docs-keeper.md` prescribe una línea `Cerrada: <fecha>` en `PLAN_TAREAS.md` que la cabecera del fichero no declara y que T00 y T01 no llevan: ¿se declara el campo en la cabecera y se regulariza, o se elimina de `docs-keeper.md`? · opciones vistas: declarar el campo, eliminar la prescripción
- [x] T10/T41 · `CLAUDE.md` se contradice sobre `failed` · **resuelto 2026-09-17 por el autor: `failed` se añade como quinto estado de `Item`**, transición `new → failed`, terminal. El § "Modelo de dominio" de `CLAUDE.md` queda corregido en consecuencia. Motivo del rechazo de las alternativas: dejar el `Item` en `new` lo convierte en un ítem envenenado permanente, porque `next_unread` lo devolvería cada noche y gastaría `max_calls_per_item` llamadas para siempre; el contador de intentos traslada a T44 la lógica de selección y deja el campo sin consumidor durante toda T41. El enum se materializa como `VARCHAR(20)` + `CHECK` (no tipo nativo), así que la migración recrea la constraint y es reversible.
- [ ] T10 · `RunStatus.RUNNING` es un quinto estado que `CLAUDE.md` no enumera; se añadió porque los cuatro documentados son terminales y T44 necesita crear el `Run` al empezar la noche · opciones vistas: dejarlo y corregir `CLAUDE.md`; otro modelado
- [ ] T10/T41 · Valores exactos de `AgentCall.status`, que `CLAUDE.md` declara sin enumerar; T10 usa `ok`/`invalid_output`/`error`/`timeout` · opciones vistas: mantenerlos (cambiarlos tras T11 exige migrar el enum); otro conjunto
- [ ] T10/T44 · Coste perdido en la ruta de error, parcialmente resuelto en T40–T41 · T40 documentó que `LLMError`/`LLMTimeout` transportan `tokens_in`/`tokens_out` ya conocidos; T41 registra `CancelledError` con sus tokens como atributos antes de relanzar (`ReadItem` captura, contabiliza, relanza). **Queda abierto el caso sin `ResultMessage`** (timeout de socket, cancelación externa previa a respuesta): no hay fuente de verdad, se contabiliza cero. Cifra: ~600.000–800.000 tokens/noche, acotada por `CALL_LIMIT_REACHED`. Mitigaciones: (a) contador de fallos consecutivos que cierre como `partial` a 5 seguidos (~50.000 tokens); (b) contabilidad pesimista; (c) bajar tolerancia en T60. Seguimiento en T44 y calibración en T60.
- [ ] T10 · ¿El dominio normaliza los `datetime` a UTC, o solo exige que sean *aware*? T10 solo exige aware · opciones vistas: normalizar en `__post_init__`; dejar la normalización a la persistencia
- [x] T10/T11 · Repositorios síncronos conviviendo con `LLMProvider` async · resuelto 2026-09-16: mantener la mezcla (SQLAlchemy sync es lo instalado, el pipeline es batch monohilo)
- [ ] T10 · `Reading` sin `created_at` · opciones vistas: añadirlo si hace falta trazabilidad propia; bastan `AgentCall.duration_ms` y el `Run`
- [ ] T11/T30 · Política de recuperación de un `Run` huérfano: el índice único parcial impide dos `running`, así que un proceso muerto sin cerrar bloquea la noche siguiente con `IntegrityError` · opciones vistas: retomar con `current()`; cerrarlo como `killed` y abrir uno nuevo
- [x] T11/T30 · `count_for_run` cuenta llamadas de cualquier `status`, incluidas `invalid_output`. Si T30 implementa el tope del Editor como `count_for_run(run_id, EDITOR) >= 1 → no llamar`, un Editor cuya primera respuesta sea JSON inválido pierde el reintento que `CLAUDE.md` le concede y esa noche no se publica nada · resuelto 2026-09-16: `max_editor_calls_per_night = 2` (default), contando **todos** los intentos de cualquier status. El gasto (tokens) sale del intento, no del éxito. ADR 0005 § 9.
- [ ] T11 · Índice `agent_calls(run_id, agent)` compuesto frente al `agent_calls(run_id)` que pide `.claude/agents/database.md` · opciones vistas: mantener el compuesto (cubre ambas consultas por prefijo izquierdo); añadir el simple
- [x] T11/T41 · Unicidad de `Reading` por `Item` · resuelto 2026-09-17: mantener la unicidad. `ReadItem` comprueba `status is NEW` antes de autorizar, así que el índice único nunca se alcanza en fase 1 (no se relee un ítem). El mecanismo es defensivo.
- [ ] T11 · `Run.tokens_used` como columna almacenada frente a valor derivado de `agent_calls` · opciones vistas: mantenerla y documentarla como caché (lo hecho); recalcularla en `save`; eliminarla
- [ ] T11 · Excepción al guardar una entidad que no existe en la base: hoy `LookupError` de la estándar · opciones vistas: `LookupError`; una `EntityNotFound` propia
- [ ] T11 · El PostgreSQL de compose publica en el puerto 5433, no 5432, porque en la máquina del autor hay un PostgreSQL nativo que posee `127.0.0.1:5432` y tapa al contenedor · opciones vistas: mantener 5433 por defecto (lo hecho, con `POSTGRES_PORT` para cambiarlo); volver a 5432 si el nativo desaparece
- [x] T02 · `tester.md` exige `addopts = "-m 'not manual'"` en `pyproject.toml`, pero nada se lo indica a `backend`, que es quien lo escribe: ¿se resuelve en el plan de T02? · resuelto 2026-09-16: `addopts = "-m 'not manual'"` y registro del marcador `manual` fijos en `backend/pyproject.toml` antes del primer test
- [ ] T02 · ¿Qué día y hora reinicia el límite semanal de la suscripción? (`budget.weekly_reset_weekday`, `weekly_reset_hour`) · opciones vistas: lunes 00:00 (placeholder actual, inerte mientras `reset_day_multiplier = 1.0`); observarlo en Settings > Usage durante T60
- [ ] T02 · ¿Es correcto `budget.editor_reserve_tokens = 60000` (20% del presupuesto nocturno)? · opciones vistas: mantenerlo y calibrarlo en T60; otro porcentaje
- [ ] T02/T30 · `Settings.config_path` está declarado pero nadie lo usa: `load_pipeline_config` calcula su propia ruta · opciones vistas: borrar el campo; cablearlo de forma consciente y documentada (útil en tests, peligroso fuera)
- [x] T02/T30 · Zona horaria de la ventana: `CLAUDE.md` dice "hora local" · resuelto 2026-09-16: clave IANA explícita `window.timezone` en el TOML. Sistema cron y contenedores no siempre propagan `TZ`. ADR 0005 § 12.
- [ ] T41/T44 · Un `Item` que falla por `RATE_LIMITED`/`TIMEOUT`/`AGENT_ERROR` vuelve a `next_unread` indefinidamente · No se marca `FAILED` (eso se reserva al JSON que no valida). Cuesta una llamada por noche, no `max_calls_per_item`. Nadie cuenta noches fallidas consecutivas · opciones vistas: contador en `Item` de fallos consecutivos; derivarlo de `AgentCall` en T44; marcar `discarded` automático tras N noches.
- [ ] T41 · `run-item` crea un `Run` nuevo con presupuesto completo en cada invocación · Un bucle de shell sobre `run-item` gasta sin tope de noche; solo lo acota `hard_stop`. Se descartaron «descontar lo ya consumido» y «reutilizar el último `Run`» porque exigen definir qué es «la noche en curso» para un `Run` ya cerrado, y `pipeline.toml` no tiene ningún campo que lo resuelva · opciones vistas: mitigación actual: aviso explícito por `stderr` al crear el `Run`.
- [x] T02/T30 · ¿Puede `application/budget.py` importar `BudgetConfig`/`LimitsConfig`/`WindowConfig` de `infrastructure/config.py` solo para anotar? · resuelto 2026-09-16: no. `BudgetPolicy` es `dataclass` estándar propia, traducción vive en `cli.py` (composition root). Evita validación redundante y mantiene `application/` desacoplada. ADR 0005 § 13. `pydantic` se añadió a la lista de prohibidos del test `test_domain_purity.py` para `application/`.
- [x] T02/T40 · Modelos por rol como alias (`"sonnet"`, `"opus"`) frente a IDs de modelo fijados · resuelto 2026-09-17: `ResultMessage.model` expone el ID de modelo efectivo (e.g. `claude-sonnet-5`), también disponible en `model_usage` (dict con claves de modelo real). `AgentResult.model` se obtiene del alias de configuración, porque T41–T44 ya conocen el rol. Alternativa: leer IDs efectivos de `model_usage` para validar que el modelo pedido coincide con el otorgado; no aplicable en fase 1 (futura si el SDK cambia los alias).
- [ ] T02 · CLAUDE.md "Estado actual" está fuera de fecha (dice "Fase 0: base documental. Sin código.") · opciones vistas: actualizar en T02; dejar como decisión abierta para siguiente sesión
- [ ] T20 · Categorías arXiv iniciales · opciones vistas: `astro-ph.EP` + `astro-ph.GA`; añadir `astro-ph.HE`. T20 las consume desde `pipeline.toml` sin conocerlas en código; `--categories` permite probar `astro-ph.HE` una noche sin editar el fichero
- [ ] T20/T44 · Los ítems `new` que nunca se leen se acumulan · la ingesta no está limitada por `max_items_per_night` a propósito; si arXiv da 150/noche y el Reader lee 40, quedan 110 por noche que `next_unread` drenará por antigüedad, retrasando cada vez más la actualidad de lo publicado · opciones vistas: `next_unread` por `published_at` descendente; caducar los `new` con más de N días; aceptar la cola. **Es estructural para T44.**
- [ ] T20 · Valores `page_size = 100` y `max_results_per_fetch = 400` · propuestos a ojo en T20, pendientes de confirmar con los primeros `--dry-run` reales · opciones vistas: mantenerlos; ajustarlos con datos
- [ ] T20 · `--dry-run` escribe en base de datos · lo "dry" es la ausencia de LLM, no de escritura. `CLAUDE.md` lo describe como "ingesta + plan de gasto" y el nombre sugiere lo contrario · opciones vistas: mantener el nombre y documentarlo; renombrarlo
- [ ] T20/T44 · `run-night` sin `--dry-run` devuelve código 2 hasta que T44 exista · opciones vistas: mantenerlo; devolver 0 sin hacer nada
- [ ] T20 · `cli.py` usa `print()` para el informe, mientras `.claude/agents/backend.md` prohíbe `print` y pide logging JSON · la interpretación aplicada es que esa regla rige la telemetría del pipeline (T44), no la salida legible de un comando interactivo · opciones vistas: mantener la distinción; unificar todo en logging estructurado
- [ ] T20 · `since` por defecto es ayer 00:00 UTC · opciones vistas: mantenerlo; día natural UTC; desde el `finished_at` del último `Run` cuando T44 exista
- [ ] T20 · Revisiones de un paper ya ingerido · con `external_id` sin versión, una `v2` no vuelve a entrar y su contenido nuevo se ignora · opciones vistas: ignorarlas; reingerir si `updated` cambia
- [ ] T20 · `User-Agent` neutro `nocturna/0.1.0` · la política de arXiv sugiere identificarse con un contacto, pero eso expondría el correo del autor o una URL cuya visibilidad es a su vez decisión abierta · opciones vistas: mantenerlo neutro; añadir contacto
- [ ] T20 · `xml.etree` frente a `defusedxml` para parsear respuesta externa · hay tope de bytes, pero `xml.etree` es susceptible a expansión de entidades · opciones vistas: mantener `xml.etree` con el tope; añadir `defusedxml`
- [x] T20/T41 · ¿`fetch_new` debe entrar en `allowed_tools` de algún agente? · resuelto 2026-09-17: ninguna herramienta expuesta en fase 1. La ingesta la hace el orquestador; el Reader no invoca `fetch_new`.
- [ ] T30/T44 · `record_call` sobre un `Run` ya cerrado persiste el `AgentCall` pero no incrementa `Run.tokens_used`, que queda corto · ninguna decisión del guard se ve afectada (lee `agent_calls`), pero la calibración de T60 leería un número corto · opciones vistas: dejarlo y recalcular desde `agent_calls` en los informes; contabilizar antes de cerrar el Run en T44; reconciliar el campo al cerrar
- [ ] T30/T44 · ¿Conviene registrar (log o métrica) cuándo se captura `RunAlreadyFinished`? · opciones vistas: dejarlo solo documentado en código; instrumentarlo en T44
- [ ] T30 · Valores `max_editor_calls_per_night = 2` y `max_calls_per_item = 2`, a calibrar en T60 · opciones vistas: mantenerlos (default conservador); ajustar según primeras noches
- [ ] T30 · ¿La reserva del Editor debe escalar con `reset_day_multiplier` en noche de reset? · Hoy no: el presupuesto sube y la reserva no, lo conservador · opciones vistas: dejarlo así; escalar la reserva también
- [ ] T30 · Semántica de `weekly_reset_hour`: hoy es "hora del día a partir de la cual aplica el multiplicador" · con ventana 00:00–04:45 cualquier valor > 4 lo convierte en código muerto · opciones vistas: mantenerlo (permite cambios dinámicos en Settings > Usage); otro modelo
- [ ] T30 · `.claude/skills/budget-guard-review/SKILL.md` busca `can_call` por nombre; ahora son `check`/`authorize` · opciones vistas: actualizar el skill; dejar como está y documentar el cambio de nombres en ADR 0005
- [x] T41 · Versionar prompts con `prompt_version` en `AgentCall` · resuelto 2026-09-17: implementado en `AgentCall`, se llena desde `AgentResult.prompt_version`, permite trazabilidad sin riesgo.
- [ ] T41 · Validación en el prompt del Reader · el revisor observó que `summary` y elementos de `objects`/`claims` no pueden ir en blanco, costando un reintento cuando el modelo los devuelve así. Una frase en `prompts/reader.md` es más barata que la llamada · opciones vistas: mantener actual (reintento automático); añadir validación de no-blanco en instrucciones.
- [ ] T42 · Idioma de los hallazgos publicados · opciones vistas: solo español; español + inglés desde el principio
- [ ] T30/T44 · Validación de `run.budget_tokens` contra `policy.nightly_tokens` · T44 crea el `Run` e inicializa `budget_tokens` con el presupuesto efectivo de la noche; T30 (`BudgetGuard`) usa ese campo como techo. Nada ata hoy ese campo a `pipeline.toml`: un `Run` creado con `budget_tokens` equivocado autorizaría gasto muy por encima del tope configurado y ningún test lo detectaría · opciones vistas: test en T44 que fije `run.budget_tokens == effective_nightly_tokens(policy, now)`; `min()` defensivo en el guard que reclasifique presupuesto al leer; ambas
- [ ] Fase 2 · Autenticación del CLI de Claude Code en el VPS · **no se resuelve en fase 1**
- [x] T40/T41 · Mecanismo fuerte para tests manuales · resuelto 2026-09-17: variable de entorno `NOCTURNA_ALLOW_REAL_CLAUDE=1` + fixture autouse en `tests/manual/conftest.py` que salta los tests si no está fijada. Cierra también lanzar pytest desde la raíz: la guarda no depende de `cwd` ni de `-m`.
- [x] T40/T41 · Salida estructurada nativa (`output_format`) o texto libre con reintento propio · resuelto 2026-09-17: texto libre con validación Pydantic + reintento manual. `ReaderOutput` valida JSON en T41; si rechaza, reintenta una vez. Pydantic se exceptúa en `application/agents/` y el test `test_domain_purity.py` comprueba que sigue vetado en el resto de `application/`.
- [x] T41 · ¿El prompt de rol viaja en `AgentRequest.prompt` o se añade un campo `system_prompt`? · resuelto 2026-09-17: campo `system_prompt` nuevo en `AgentRequest`, pasa como `system_prompt` a `ClaudeAgentOptions`. Abstract en `prompt` envuelto en `<abstract>`/`</abstract>`. Separa instrucciones de datos.
- [ ] T42 · ¿Factorizar `authorize → run_agent → record_call`** en un colaborador de `application/` cuando haya dos consumidores? · contexto: T41 y T42 heredan el mismo patrón de T40 (`test_sdk_smoke.py`). Opciones: mantenerlo duplicado en cada caso de uso (legible, explícito); una función o clase auxiliar (T43 = Editor lo necesita también en T44; a los tres es donde aplica). **A decidir cuando T41 esté pronta.**
- [ ] T40/T41/T44 · `RateLimitEvent`/`RateLimitInfo` se descartan en el bucle · el SDK los emite con `status`, `utilization` y `resets_at` incluso en llamadas exitosas (sin 429, sin error). Se observó en pruebas reales (T40 y T41): evento presente en stream, ambas llamadas completaron OK, datos de gasto íntegros. Sin consumidor en fase 1. Opciones: registrar en log `debug` para telemetría de T60 (calibración semanal); ignorar; emitir `LLMRateLimited` cuando se detecte cambio en `status` o `utilization`. **No dejar `LLMRateLimited` sin emisor ni consumidor tras T44 si se emite.**
- [ ] T40/T60 · Gasto lateral del CLI (modelo interno de control, Haiku) no aparece en `ResultMessage.usage`, solo en `model_usage` y `total_cost_usd` · observado: una llamada al Reader gastó 523 tokens visibles en `usage`, pero `model_usage` reportó además 946 tokens de Haiku (9,2% del gasto total, ~2,8× subregistro de token count). Coste fijo por sesión MCP, no por llamada; en fase 1 son ~82 sesiones/noche (40 ítems × 2 turns máximo + Editor), lo que suma ~77.572 tokens/noche invisibles en `BudgetGuard` (20,5% de déficit en el presupuesto). **No configurable desde `pipeline.toml`; debe calibrarse en T60 contra Settings > Usage, usando la identidad `total_cost_usd == sum(costUSD en model_usage)` como detector barato de un tercer sumidero si algún día aparece.**
- [ ] T40 · `LLMRateLimited` sigue sin emisor ni consumedor · `CLAUDE.md` prohíbe stubs «para el futuro». Opciones: la emite T41/T44; se borra la clase.
- [ ] T40/T41 · Ubicación de ejecución de pytest determina qué tests se recolectan · contexto: `addopts = "-m 'not manual'"` vive en `backend/pyproject.toml`. Desde `backend/`, pytest carga el fichero y deselecciona `tests/manual/`. Desde la raíz, pytest busca `pyproject.toml` en la raíz (que hereda settings globales), ignora `addopts` de `backend/`, y **recolecta el test de humo, que se ejecutaría de verdad si la variable `NOCTURNA_ALLOW_REAL_CLAUDE` no estuviera fijada**. Evidencia: desde raíz salen 455 recolectados; desde `backend/`, 454 + 1 deselected. Variable de entorno `NOCTURNA_ALLOW_REAL_CLAUDE=1` es la única barrera real hoy; la ubicación no es una barrera confiable. Opciones: documentar que `pytest` siempre se lanza desde `backend/`; implementar `--run-manual` como flag propio que valide además la variable. **Refuerza la necesidad de fijar `NOCTURNA_ALLOW_REAL_CLAUDE=1` como guarda única antes de T41 añada un segundo test manual.**
- [ ] T40 · Semántica de `allowed_tools=[]` en el SDK · resuelto por ejecución real: **`allowed_tools=[]` no apaga nada; el campo que apaga herramientas es `tools=[]` → `--tools ""`**. La guarda usa `tools=[]` + `skills=[]` explícitos para no heredar herramientas de `.claude/`. Diferencia en gasto (ADR 0006): `tools=None` (default del SDK) habría declarado el toolset del CLI en cada `query()`: **240.000–800.000 tokens/noche de preámbulo, una a casi tres noches de presupuesto**. `tools=[]` lo ata a cero. Decisión congelada por `test_agent_sdk_options.py` en T40.
