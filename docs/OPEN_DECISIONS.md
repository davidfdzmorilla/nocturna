# Decisiones abiertas

Formato: `- [ ] <tarea> · <pregunta> · opciones vistas: … ` → al resolver: `- [x] … · resuelto <fecha>: <respuesta>`.

- [x] T00 · Nombre del proyecto y del paquete Python · resuelto 2026-09-15: **Nocturna**, paquete `nocturna`
- [ ] T00 · ¿Qué estrategia de ramas sigue el proyecto? · opciones vistas: rama por tarea + PR a `main` (practicado en T00); commits directos en `main`
- [ ] T00 · ¿Qué licencia lleva el repositorio? · opciones vistas: sin licencia (privado), MIT, AGPL
- [ ] T00 · ¿El repositorio es público en GitHub o privado? · opciones vistas: público, privado
- [x] T01 · `.claude/agents/` ya está completo en el commit inicial: ¿se cierra T01 como verificación de los ocho ficheros contra `AGENTS.md`, o se reescriben? · resuelto 2026-09-16: cierre como verificación documentada más un delta mínimo de seis ediciones; los ocho ficheros ya satisfacían rol, herramientas y reglas, pero la definición de `backend` no alcanzaba para delegar T02
- [ ] T01 · ¿Qué subagente tiene la propiedad de `.claude/agents/`, `.claude/commands/`, `.claude/skills/` y `.claude/hooks/`? · opciones vistas: `docs-keeper` (practicado en T01, por ser markdown de proceso), `backend`, subagente nuevo
- [ ] T01 · `Bash(git commit*)` no está en `allow` ni en `deny` de `.claude/settings.json`, así que el commit dispara prompt al autor: ¿es deliberado como refuerzo de "preparar ≠ ejecutar"? · opciones vistas: dejarlo así, añadirlo a `allow`
- [ ] T01 · `docs-keeper.md` prescribe una línea `Cerrada: <fecha>` en `PLAN_TAREAS.md` que la cabecera del fichero no declara y que T00 y T01 no llevan: ¿se declara el campo en la cabecera y se regulariza, o se elimina de `docs-keeper.md`? · opciones vistas: declarar el campo, eliminar la prescripción
- [ ] T10/T41 · `CLAUDE.md` se contradice sobre `failed`: § "Modelo de dominio" enumera `new/read/discarded/published`, pero § "Agentes" y T41 dicen "el ítem se marca `failed`" · opciones vistas: añadir `failed` como quinto estado de `Item`; interpretar que `failed` es el `status` de `AgentCall` y dejar el `Item` en `new` o `discarded`; contador de intentos en `Item`. **A resolver antes de T41.**
- [ ] T10 · `RunStatus.RUNNING` es un quinto estado que `CLAUDE.md` no enumera; se añadió porque los cuatro documentados son terminales y T44 necesita crear el `Run` al empezar la noche · opciones vistas: dejarlo y corregir `CLAUDE.md`; otro modelado
- [ ] T10/T41 · Valores exactos de `AgentCall.status`, que `CLAUDE.md` declara sin enumerar; T10 usa `ok`/`invalid_output`/`error`/`timeout` · opciones vistas: mantenerlos (cambiarlos tras T11 exige migrar el enum); otro conjunto
- [ ] T10/T44 · Coste perdido en la ruta de error: si `run_agent` lanza `LLMTimeout` o `LLMRateLimited` no hay `AgentResult`, así que el `AgentCall` se registrará con `tokens_in=0`. Una llamada que sí consumió tokens se contabiliza como gratis · opciones vistas: que las excepciones transporten los tokens consumidos; asumir el subregistro y documentarlo. **Es control de gasto: no debe descubrirse en T44.**
- [ ] T10 · ¿El dominio normaliza los `datetime` a UTC, o solo exige que sean *aware*? T10 solo exige aware · opciones vistas: normalizar en `__post_init__`; dejar la normalización a la persistencia
- [x] T10/T11 · Repositorios síncronos conviviendo con `LLMProvider` async · resuelto 2026-09-16: mantener la mezcla (SQLAlchemy sync es lo instalado, el pipeline es batch monohilo)
- [ ] T10 · `Reading` sin `created_at` · opciones vistas: añadirlo si hace falta trazabilidad propia; bastan `AgentCall.duration_ms` y el `Run`
- [ ] T11/T30 · Política de recuperación de un `Run` huérfano: el índice único parcial impide dos `running`, así que un proceso muerto sin cerrar bloquea la noche siguiente con `IntegrityError` · opciones vistas: retomar con `current()`; cerrarlo como `killed` y abrir uno nuevo
- [ ] T11/T30 · `count_for_run` cuenta llamadas de cualquier `status`, incluidas `invalid_output`. Si T30 implementa el tope del Editor como `count_for_run(run_id, EDITOR) >= 1 → no llamar`, un Editor cuya primera respuesta sea JSON inválido pierde el reintento que `CLAUDE.md` le concede y esa noche no se publica nada · opciones vistas: contar solo las llamadas con `status = ok`; contar todas y aceptar que el reintento del Editor no existe
- [ ] T11 · Índice `agent_calls(run_id, agent)` compuesto frente al `agent_calls(run_id)` que pide `.claude/agents/database.md` · opciones vistas: mantener el compuesto (cubre ambas consultas por prefijo izquierdo); añadir el simple
- [ ] T11/T41 · Unicidad de `Reading` por `Item`: el índice único hace que `run-item` sobre un ítem ya leído falle también en la base · opciones vistas: mantener la unicidad; permitir varias lecturas y que `get_for_item` devuelva la última
- [ ] T11 · `Run.tokens_used` como columna almacenada frente a valor derivado de `agent_calls` · opciones vistas: mantenerla y documentarla como caché (lo hecho); recalcularla en `save`; eliminarla
- [ ] T11 · Excepción al guardar una entidad que no existe en la base: hoy `LookupError` de la estándar · opciones vistas: `LookupError`; una `EntityNotFound` propia
- [ ] T11 · El PostgreSQL de compose publica en el puerto 5433, no 5432, porque en la máquina del autor hay un PostgreSQL nativo que posee `127.0.0.1:5432` y tapa al contenedor · opciones vistas: mantener 5433 por defecto (lo hecho, con `POSTGRES_PORT` para cambiarlo); volver a 5432 si el nativo desaparece
- [x] T02 · `tester.md` exige `addopts = "-m 'not manual'"` en `pyproject.toml`, pero nada se lo indica a `backend`, que es quien lo escribe: ¿se resuelve en el plan de T02? · resuelto 2026-09-16: `addopts = "-m 'not manual'"` y registro del marcador `manual` fijos en `backend/pyproject.toml` antes del primer test
- [ ] T02 · ¿Qué día y hora reinicia el límite semanal de la suscripción? (`budget.weekly_reset_weekday`, `weekly_reset_hour`) · opciones vistas: lunes 00:00 (placeholder actual, inerte mientras `reset_day_multiplier = 1.0`); observarlo en Settings > Usage durante T60
- [ ] T02 · ¿Es correcto `budget.editor_reserve_tokens = 60000` (20% del presupuesto nocturno)? · opciones vistas: mantenerlo y calibrarlo en T60; otro porcentaje
- [ ] T02/T30 · `Settings.config_path` está declarado pero nadie lo usa: `load_pipeline_config` calcula su propia ruta · opciones vistas: borrar el campo; cablearlo de forma consciente y documentada (útil en tests, peligroso fuera)
- [ ] T02/T30 · Zona horaria de la ventana: `CLAUDE.md` dice "hora local" · opciones vistas: hora local del sistema (comportamiento actual, sin clave); clave IANA explícita `window.timezone` en el TOML
- [ ] T02/T30 · ¿Puede `application/budget.py` importar `BudgetConfig`/`LimitsConfig`/`WindowConfig` de `infrastructure/config.py` solo para anotar? · opciones vistas: importar desde infraestructura (rompe la flecha `application → domain` aunque sean datos puros); mover el esquema a `application/` y dejar solo el lector en `infrastructure/`
- [ ] T02/T40 · Modelos por rol como alias (`"sonnet"`, `"opus"`) frente a IDs de modelo fijados · opciones vistas: mantener alias (actual); fijar IDs exactos cuando T40 consulte la documentación vigente del SDK
- [ ] T02 · CLAUDE.md "Estado actual" está fuera de fecha (dice "Fase 0: base documental. Sin código.") · opciones vistas: actualizar en T02; dejar como decisión abierta para siguiente sesión
- [ ] T20 · Categorías arXiv iniciales · opciones vistas: `astro-ph.EP` + `astro-ph.GA`; añadir `astro-ph.HE`. T20 las consume desde `pipeline.toml` sin conocerlas en código; `--categories` permite probar `astro-ph.HE` una noche sin editar el fichero
- [ ] T20/T44 · Los ítems `new` que nunca se leen se acumulan · la ingesta no está limitada por `max_items_per_night` a propósito; si arXiv da 150/noche y el Reader lee 40, quedan 110 por noche que `next_unread` drenará por antigüedad, retrasando cada vez más la actualidad de lo publicado · opciones vistas: `next_unread` por `published_at` descendente; caducar los `new` con más de N días; aceptar la cola. **Es estructural para T44.**
- [ ] T20 · Valores `page_size = 100` y `max_results_per_fetch = 400` · propuestos a ojo en T20, pendientes de confirmar con los primeros `--dry-run` reales · opciones vistas: mantenerlos; ajustarlos con datos
- [ ] T20 · `--dry-run` escribe en base de datos · lo "dry" es la ausencia de LLM, no de escritura. `CLAUDE.md` lo describe como "ingesta + plan de gasto" y el nombre sugiere lo contrario · opciones vistas: mantener el nombre y documentarlo; renombrarlo
- [ ] T20/T44 · `run-night` sin `--dry-run` devuelve código 2 hasta que T44 exista · opciones vistas: mantenerlo; devolver 0 sin hacer nada
- [ ] T20 · `cli.py` usa `print()` para el informe, mientras `.claude/agents/backend.md` prohíbe `print` y pide logging JSON · la interpretación aplicada es que esa regla rige la telemetría del pipeline (T44), no la salida legible de un comando interactivo · opciones vistas: mantener la distinción; unificar todo en logging estructurado
- [ ] T20 · `since` por defecto es ayer 00:00 UTC · opciones vistas: mantenerlo; día natural UTC; desde el `finished_at` del último `Run` cuando T44 exista
- [ ] T20 · Revisiones de un paper ya ingerido · con `external_id` sin versión, una `v2` no vuelve a entrar y su contenido nuevo se ignora · opciones vistas: ignorarlas; reingerir si `updated` cambia
- [ ] T20 · `User-Agent` neutro `nocturna/0.1.0` · la política de arXiv sugiere identificarse con un contacto, pero eso expondría el correo del autor o una URL cuya visibilidad es a su vez decisión abierta · opciones vistas: mantenerlo neutro; añadir contacto
- [ ] T20 · `xml.etree` frente a `defusedxml` para parsear respuesta externa · hay tope de bytes, pero `xml.etree` es susceptible a expansión de entidades · opciones vistas: mantener `xml.etree` con el tope; añadir `defusedxml`
- [ ] T20/T41 · ¿`fetch_new` debe entrar en `allowed_tools` de algún agente? · En fase 1 nadie la llama: la ingesta la hace el orquestador · opciones vistas: exponer solo `get_abstract` (recomendado por gasto); exponer ambas
- [ ] T41 · Versionar prompts con `prompt_version` en `AgentCall` · recomendado, coste bajo
- [ ] T42 · Idioma de los hallazgos publicados · opciones vistas: solo español; español + inglés desde el principio
- [ ] Fase 2 · Autenticación del CLI de Claude Code en el VPS · **no se resuelve en fase 1**
